# Project Memory

Specification: [DESIGN.md](../documentation/DESIGN.md) (root system design) and [DESIGN.md](DESIGN.md) (this project's requirements and use cases, UC1-UC16).

- This project is the Svelte + TypeScript management UI.
- The API defaults to `http://127.0.0.1:5080`; override it with `VITE_API_BASE_URL`.
- Authentication uses the API's JWT token and local storage.
- The UI supports English and Dutch and remembers the selected language.
- Never hardcode user-facing labels in views/components; add them to `src/lib/i18n.ts` for every language instead (see [CLAUDE.md](CLAUDE.md)).
- Date-only values must be displayed with local calendar semantics; use the shared date utility.
- Main management areas are overview, matches, competitions, participants, categories, templates, accounts (UC16), profile (UC14), and tenant administration/sub-tenants (UC15).
- Accounts management (UC16) is tenant-administrator only: list shows username/real name/authorization, edit screen changes username, real name, e-mail, password (only if non-empty) and authorization; a user cannot change their own authorization. Backed by `GET/PUT /api/accounts/{id}`; `PUT` enforces the same username-required/username-uniqueness checks as `POST` (excluding the account's own id), surfaced via `error_USERNAME_REQUIRED`/`error_USERNAME_TAKEN`. "Add account" only asks for a username and creates an unprivileged (`Viewer`) account via `POST /api/accounts` (server generates a random initial password); on failure the add-form stays open with a coded error message.
- API errors that the UI must react to specifically (login, change-password, account creation) come back as `{ code, message }` instead of plain text. `api.ts`'s `ApiRequestError` carries `code`; never show the raw `message` to users. Use `labelForError(error, labels, fallbackKey)` from `src/lib/errors.ts`, which looks up an `error_<CODE>` key in `i18n.ts` (add one per language) and falls back to `fallbackKey` for unknown/network errors. Extend this pattern (`ApiError`/`error_<CODE>` keys) whenever a new endpoint's failures need specific UI messaging.
- The header shows the active tenant's own name/logo (falling back to the default "Centaur Scores" branding) and a profile button/avatar next to log out.
- Sub-tenant logos are uploaded client-side (SVG/PNG, 1:1 preferred aspect ratio, 256KB max) and stored as data URLs via the API; the `tenants.logo_url` column is `longtext`.
- Category management (UC5) and participant list management (UC7) each have dedicated list/detail views (`CategoriesView`/`CategoryDetailView`, `ParticipantListsView`/`ParticipantListDetailView`/`ParticipantMemberView`).
- Match template management (UC6, `TemplatesView`/`TemplateEditView`) was the last generic-stub section; the old `ManagementView.svelte`/`managementData.ts` generic fallback has been removed entirely now that all four tenant-home sections (participants, categories, templates, accounts) have dedicated screens.
- `MatchTemplate.configurationJson` is a free-form JSON blob the backend never parses/validates; its shape (`categoryOrder`, ordered `deviceNames`, `keyboard`, `disabledKeyRules`, `scoringRules`, `liveScopes`) is defined and owned entirely by the frontend in `src/lib/templateConfig.ts` (parse/default helpers) and `src/lib/types.ts` (`TemplateConfiguration` and friends). `TemplateEditView` manages standard device names and order; `MatchesView` creates those devices sequentially after creating a match from the template. Keyboard keys are `{ keyId, label, color, value }`, all fields required (the old delete/wipe key concept with nullable fields was removed; scorekeeper apps add their own delete key if needed). Keyboard keys can be reordered in `TemplateEditView`/`MatchMetadataEditView` via ▲/▼ move buttons. Scoring rules are an ordered list of `{ type: 'total' | 'countKey', keyId? }` tie-breakers. Live scopes mirror the real `LiveScoreScope` entity's fields.
- Categories and participant lists (with members) are eager-loaded in `App.svelte`'s `loadData()` alongside matches/competitions, since the backend already returns nested values/members in one call; after any mutation, call `refreshCategories()`/`refreshParticipantLists()`/`refreshTemplates()` to refetch rather than mutating local state.
- Participant list members have a per-member `isActive` flag (UC7); inactive members/lists are tagged and sorted after active ones. Every defined category must have a selected value before a member can be saved.
- Match management (UC8, `MatchesView`/`MatchDetailView`/`MatchMetadataEditView`/`MatchDevicesView`/`MatchParticipantView`/`MatchQrCodesView`) reuses the template config split: `Match.keyboardJson` holds `{ categoryOrder, keyboard, disabledKeyRules }` and `Match.scoringRulesJson` holds the ordered `ScoringRule[]` (see `src/lib/matchConfig.ts`); live scopes are real `LiveScoreScope` rows on the match, unlike the template's JSON blob.
- **Participant selection is a per-match/per-template concept, not per-device.** `Match`/`MatchTemplate` both carry `participantListId` (nullable — which participant list a match/template draws from) and `deviceSelectionMode: 'restricted' | 'list' | 'list-and-free'` (which participants a scoring device may choose between at score-entry time: only already-added match participants / freely from the list / list + already-added + free entry). `ScoreDevice` itself is now just `{ id, matchId, name }` — no mode, no participant-order JSON; `MatchDevicesView` is just a name-only add/remove list. `Match.allowFreeParticipants` controls whether ad-hoc (non-list) participants can be added to the match at all.
- Once a match has any participants, its `participantListId` is locked (edit UI disables the select and shows `participantListLockedHint`; backend returns 409 `PARTICIPANT_LIST_LOCKED` if you try to change it anyway via `PUT /api/matches/{id}`).
- **Universal list-rendering rule**: whenever showing a participant-list member for selection (add-participant dropdowns, etc.), always render "full name (concatenated category values)" using the full tenant `categories` list (not a match/template's restricted `categoryOrder`), and always exclude members with `isActive === false` from selection dropdowns — this is a general rule stated by the user, not scoped to one screen.
- `MatchDetailView`'s add-participant form no longer lets the user pick a source list — the source list is fixed to `match.participantListId`; the add-mode (list vs. manual) is derived automatically from whether a source list exists and `match.allowFreeParticipants`.
- "Add match" only asks for name/date/optional template; ends/arrowsPerEnd default to 10/3 (templates don't define them) and are editable afterwards in `MatchMetadataEditView`. Choosing a template copies its keyboard/rules/live-scopes onto the new match and opens the match home directly; no template opens `MatchMetadataEditView` for ad-hoc setup instead.
- The match home screen (`MatchDetailView`) is participant-list-first per UC8, with sort (name/score) and group (none/category/device) toggles, inline add-participant (from any participant list, or manual entry when `allowFreeParticipants`), and function buttons for metadata/devices/QR codes/CSV export/results-per-scope/delete (confirmed). A results scope opens the anonymous `/narrowcast/{scope}` display (no tenant in the URL — it rotates through live matches from *every* tenant that has an open match using that scope name), which rotates through data from `/live-scoring/match/{scope}` and `/live-scoring/match/{scope}/{matchId}`.
- The printable QR codes screen (`MatchQrCodesView`, route `/matches/{id}/qr`) renders without the app header, 4 bordered blocks per printed page, each with the match name, device name, and a QR code (via the `qrcode` npm package) encoding `${apiBase}/scorekeeper/{tenantId}/{matchId}/{deviceId}` — this endpoint already exists unauthenticated in `PublicScoresController`.
- A participant's assigned score device is set via `PUT /api/matches/{id}/participants/{participantId}/device` (added for UC8); assigning clears the participant from every other device's restricted participant list, matching the "one device per participant" rule in the root DESIGN.md.
- `MatchParticipantView` lets managers edit a match participant's metadata in place (`PUT /api/matches/{id}/participants/{participantId}`, reuses `CreateMatchParticipantRequest`'s shape) — either manually (lastName/fullName/federationNumber/categories) or by "replacing" the participant with another member from the match's source `participantListId` (excludes members already assigned elsewhere in the match and inactive members, per the universal list-rendering rule above). The update response doesn't include `scores`, so the frontend merges only the metadata fields locally and keeps existing scores until the next full refresh. Requires passing `participantLists` into `MatchParticipantView` (from `App.svelte`). **A source-list participant no longer stores its own name/number/categories server-side (2026-09-08 API change)** — `fullName`/`lastName`/`federationNumber`/`categories` on any `MatchParticipant` with `participantListMemberId` set are now resolved live from the linked `ParticipantListMember` on every read, so editing the roster entry (`ParticipantListsView`) propagates into every match using it without re-adding the participant. The lastName/fullName/federationNumber/categories fields this view sends when relinking to a list member are ignored server-side (harmless to keep sending); they only take effect when converting the participant to free (`participantListMemberId: null`). No frontend code changed for this — the JSON field names/shape are unchanged, only how the API produces the values.
- Score entry (`setScore`) and quick-set-total (`applyQuickSet`) in `MatchParticipantView` optimistically reassign the local `participant` prop with the new/replaced scores before awaiting the API calls, so totals/running totals/dropdown selections update immediately instead of waiting on the full match refresh; `onChanged()` is still awaited afterwards to reconcile with the server.
- `MatchParticipantView` shows an "Add to {list name}" button next to "Edit details" for a free/unlisted participant (`!participant.participantListMemberId`) when the match has a linked source list (`sourceList` non-null) and the signed-in user `canManage`. `canManage` (computed once in `App.svelte`, `isAdmin || profile.authorization === 'Manager'`) had to be threaded in as a new prop — this view didn't previously receive it. It's a real `<a href={participantMemberPath(sourceList.id, 'new')}>` reusing the participant-list screen's own "+ New participant" route/form (per the navigation convention above), but wired through `navigateOnClick` to an `onAddToList` callback rather than `target="_blank"`, because the flow needs in-app state, not just a URL: `App.svelte`'s `beginAddParticipantToList()` stashes `pendingParticipantLink { matchId, participantId }` and `memberPrefill { fullName, federationNumber, categories }` (new top-level `let`s, deliberately *not* reset by `applyRouteResult()` on the navigation) before navigating to the add-member screen. `ParticipantMemberView` gained an optional `prefill` prop (same shape) that seeds its blank-member init path (`member?.x ?? prefill?.x ?? ''`) only when creating (`member` is `null`) — editing an existing member is unaffected. Its `onSaved` callback signature changed from `() => void` to `(member: ParticipantListMember) => void` so the newly created member (id/fields) is available without a second fetch — `save()` now captures the API response (`addParticipantMember`/`updateParticipantMember` both resolve the full member per the backend's `Ok(member)`) instead of discarding it. `App.svelte`'s `onMemberSaved(member)` checks `pendingParticipantLink`: if set, it calls `updateMatchParticipant(matchId, participantId, { participantListMemberId: member.id, ...member })` (same "link to member" shape `submitReplaceFromList` uses) then navigates to `matchParticipantPath(matchId, participantId)` — landing back on the originating match participant screen, now list-linked — instead of the normal `/participants/{listId}` destination; `onMemberBack`/`onMemberDeleted` were split out from the old shared `onMemberSaved` handler to apply the same "return to origin if pending" logic (back) or always clear pending state (delete, which can't happen mid this flow but guards future reuse). `addMember()`/`openMember()` (the normal "+ New participant"/edit-member entry points) explicitly clear both pending variables so a stale detour never leaks prefill/link-back behavior into an unrelated add or edit.
- Known UC8 gaps (backend has no support, intentionally not implemented): assigning arbitrary accounts as "match admins" — there's no such entity/endpoint yet.
- UI tests use Vitest; type checking uses `svelte-check`.
- Run `npm test`, `npm run check`, and `npm run build` after frontend changes (also documented in CLAUDE.md).
- `Match.participantCount` was a dead frontend-only field the API never populated (a stray `participant_count` DB column was even dropped from the API's `matches` table as unrelated cruft — see `RemoveStrayMatchParticipantCountColumn` migration). Both `GET /api/matches` and `GET /api/matches/{id}` already eager-load and serialize the full `Participants` collection per match, so `matchParticipantCount(match)`/`matchUnlistedParticipantCount(match)` in `src/lib/matchParticipants.ts` just derive counts from `match.participants` client-side (length, and members without a `participantListMemberId` for manually-added/free entries) — no backend change was needed. `MatchesView`'s full list surfaces a red "N unlisted" indicator next to the count as an operator trigger-to-act; `HomeView`'s open-matches panel only shows the corrected total.
- Participant list Excel export/import (`ParticipantListDetailView`) is generated/parsed entirely server-side (`GET /api/participant-lists/{id}/export.xlsx?language=`, `POST .../import.xlsx`) — the frontend just triggers a blob download (mirrors `downloadMatchExport`) and posts a `FormData` upload (`api.importParticipantList`), and shows the returned `{ created, updated, warnings }` summary. Both buttons are gated on a new `canManage` prop (`isAdmin || authorization === 'Manager'`, computed once in `App.svelte` and passed down) since export/import — like every other write on this screen — is a manager-or-higher action; unlike other manage actions in this codebase (which just rely on the backend's 403), this pair is deliberately hidden from viewers in the UI too, per explicit instruction. Coded import failures reuse `labelForError` with `error_IMPORT_*` keys (`IMPORT_MISSING_SHEET`, `IMPORT_UNRECOGNIZED_HEADERS`, `IMPORT_INVALID_FILE`, `IMPORT_FILE_MISSING`).
- All list-row/nav "navigate to a child view" controls (matches, competitions, participant lists/members, tenants, accounts, categories, templates, home dashboard, header nav, and the match QR-codes/results actions that used to call `window.open`) were converted from `<button on:click>` to `<a href>` so right-click/middle-click/ctrl-click can open the target in a new tab — see the "Navigation to another route" convention in CLAUDE.md. `router.ts` gained one path-builder function per route (`matchPath`, `competitionPath`, `participantMemberPath`, `matchResultsPath`, etc., used by both `App.svelte` and the view components) and a `navigateOnClick(event, action)` helper (plain left click → `preventDefault()` + run the existing SPA `navigate()` callback; modifier/middle click → let the browser open the `href` natively in a new tab, which does a normal fresh page load there — same-tab clicks stay reload-free). `app.scss` got a global `a { text-decoration: none; color: inherit; }` reset plus `a` variants added next to a few tag-qualified `button` selectors (`nav button`, `.quick-links button`) so the converted anchors keep the exact prior look. Found but deliberately left alone: `App.svelte`'s `signIn()` calls `navigate('/')` only after `loadData()` resolves, so a very fast click during that window can get clobbered by that trailing redirect — a pre-existing race, not reachable by a human clicking normally, unrelated to this change.
- The home screen's bottom nav (`HomeView.svelte`'s old plain `.quick-links` buttons for participants/categories/templates/accounts/tenants/backup-restore/personal-best) was converted to the same icon+title+description tile pattern used by `BackupRestoreView`/`PersonalBestView` (`.pb-tile-grid`/`.pb-tile`/`.pb-tile-icon`/`.pb-tile-title`/`.pb-tile-description`). Those classes were previously duplicated as component-local `<style>` blocks in both those views; they're now hoisted into `app.scss` as shared global classes (`.quick-links` CSS was removed entirely) — reuse `.pb-tile-grid`/`.pb-tile` for any future icon-tile action grid instead of redefining it locally. `HomeView`'s `quickLinks` prop changed shape from `[path, label][]` to `{ path, icon, title, description }[]`; `App.svelte`'s `homeQuickLinks` builds it in a fixed order (categories, participants, templates, personal-best if visible, then admin-only accounts/tenants/backup-restore) with icons as emoji and new `homeTile*Title`/`homeTile*Description` i18n keys (some tiles reuse existing title keys like `t.categories`/`t.accounts`, others — participants/templates — got dedicated `homeTileParticipantsTitle`/`homeTileTemplatesTitle` because the tile wants a different label than that view's own page-header title).
- **Tenant-less login + multi-tenant access resolution (2026-08-29)**: `LoginView` no longer has a tenant dropdown (`login(username, password)` in `api.ts` dropped the `tenantId` param, and `fetchTenants()`/the `tenants` store in `data.ts` were removed with it). The API's login token now starts with an empty tenant claim; `App.svelte`'s `bootstrapSession()` (replacing the old `loadData()` wrapper, called from both `signIn()` and `onMount()`) fetches `GET /api/auth/me` first — via the new standalone `loadProfile(api)` in `data.ts`, split out of the eager-load batch specifically so it runs *before* every other tenant-scoped call, which now 403s (`TENANT_NOT_SELECTED`) without a resolved tenant — reads its `authorizedForTenants: TenantAccess[]` (home tenant + every hierarchy descendant, all at the account's own level), resolves one via `resolveTenantSelection()` (single tenant auto-selects; else a `localStorage['centaur-tenant']` hint if still valid; else alphabetically-first by name), and calls `api.selectTenant(tenantId)` to re-mint the token before running the rest of `loadData()`. Zero authorized tenants renders `NoTenantAccessView` (full-screen error + logout, gated by a new `tenantAccessError` flag); a `sessionReady` flag gates the full app-shell render so nothing tenant-scoped renders before `bootstrapSession()` finishes. `localStorage['centaur-tenant']` was repurposed from "login-form pre-selection" to "last-selected-tenant hint" — it's now written only once a tenant is actually resolved (never in `signIn()` itself) and deliberately never cleared on `signOut()`, so it survives to the next login. `bootstrapSession()` always calls `selectTenant()` even when the stored token already has a valid tenant — a deliberate one-round-trip cost for a single code path that also self-heals if the account's tenant tree changed since the token was minted. The header's profile menu gets a "Switch to a different tenant" item (`AppHeader`'s new `showTenantSwitch` prop, true when `authorizedForTenants.length > 1`) linking to a new `SelectTenantView` (`/select-tenant` route) showing one `.pb-tile` per tenant sorted alphabetically with logo + level, which calls `api.selectTenant` via an `onSelectTenant` callback owned by `App.svelte` (not a local API call, since a successful switch must mutate `App.svelte`'s top-level `token`/`tenant` state — the one documented exception to this codebase's usual "view calls `api.x()` then `onChanged()`" convention). The small `authorizationLabel()` helper, previously duplicated locally in `AccountsView.svelte`, was extracted to `src/lib/authorization.ts` for `SelectTenantView` to reuse.
- **Tenant-level default narrowcast scope + "claim scope" (2026-08-29)**: the tenant's effective default scope (`DefaultScopeSettings.effectiveValue` from `GET /api/tenants/current/default-scope`, resolved server-side up the tenant hierarchy) is loaded once into the `defaultScopeSettings` store in `data.ts` alongside `currentTenant`/etc. in `loadData()`, and derived in `App.svelte` as `effectiveDefaultNarrowcastScope` — passed down to `MatchesView` (the no-template "add match" fallback scope) and `TemplatesView` (new-template default via `buildEmptyTemplateConfiguration(defaultScope)`, replacing the old hardcoded `'all'`/`emptyTemplateConfiguration` constant — that constant still exists calling `buildEmptyTemplateConfiguration()` with no arg for the parse-failure fallback in `parseTemplateConfiguration`, where a fixed `'all'` is fine since that path is corrupt-JSON recovery, not new-template UX). Editing the tenant's own override lives in a new self-service `TenantSettingsView`/`tenant-settings` route (home tile gated on `canManage`, not `isAdmin` — distinct from the existing admin-only `TenantsView`/`TenantEditView`, which manage *child* tenants and also gained a `defaultNarrowcastScope` field for administrators to override on a sub-tenant). `MatchDetailView` reactively calls `api.fetchScopeConflicts(match.id)` whenever `match` changes (mirrors the existing `$: if (match.id) loadResults()` pattern — Svelte reactive statements re-run on *reassignment* of a referenced prop, not on its value changing, so this refires correctly after activate/deactivate even though `match.id` itself never changes) and, if any conflicts come back, shows a warning box (reusing the `#fdeeea`/`#e8755b` tint already used for `.unlisted-row` in the same file) listing each colliding tenant/scope/count with a "claim scope" button (`api.claimScope`) that re-checks conflicts afterward.
- **Proactive JWT-expiry watcher (2026-09-13)**: previously an expired token was only discovered reactively, the moment some mutation hit a 401 — a user could work for a while against a dead session and only find out on save. `src/lib/sessionWatch.ts` now holds the pure/testable pieces: `decodeJwtExpirySeconds`/`isTokenExpired` (base64url-decode the JWT payload, no signature verification — this is a client-side UX nicety, not a security check, the backend still enforces the real 401), `makeExpiryCache()` (memoizes the decoded expiry per token so the poll doesn't re-decode every tick), and `isSessionWatchActive(state)` — the single source of truth for *which views this must never run on*. `App.svelte` mounts `SessionExpiryWatcher.svelte` (a headless component: `onMount` checks once, `setInterval` every 15s, plus a `visibilitychange` listener for the tab-foregrounded case) at the top level, gated by `sessionWatchActive = isSessionWatchActive({ token, loggedIn, sessionReady, tenantAccessError, view })`. On expiry it does `window.alert(labels.sessionExpiredMessage)` (matches this codebase's existing no-custom-modals convention — confirms/deletes already use native `confirm()`) then calls `onExpired` (`signOut`, which clears the token and navigates to `/login`). **Deliberately excluded views** (`VIEWS_WITHOUT_SESSION_WATCH` in `sessionWatch.ts`): `narrowcast`, `match-qr`, `match-results-scope`, `competition-results` — these render standalone/unattended (kiosk displays, printed result sheets) with no one present to dismiss a popup, and `narrowcast` specifically has zero auth of its own (`fetchLiveScoringMatches`/`fetchLiveScoringPage` in `api.ts` hit the public `/live-scoring/match/...` endpoints with no bearer token at all) so it must never redirect regardless of whatever stale token happens to be sitting in that machine's `localStorage`. Tested in `sessionWatch.test.ts` including a dedicated narrowcast-path case (empty token, and an already-expired token) proving `isSessionWatchActive` stays `false` there; there's no component-render test infra in this repo (Vitest runs in the default `node` environment, no jsdom/testing-library), so the Svelte component itself was verified via a one-off Playwright smoke run instead (login → clock-jump past the real token's `exp` via `Date.now` override → poll and `visibilitychange` paths both fire the alert and redirect; narrowcast confirmed silent under both no-token and expired-token `localStorage` states).
- **Bulk table for "add from participant list" in `MatchDetailView` (2026-09-13)**: the single-select `<select>` dropdown for adding one existing list member at a time was replaced with `src/lib/ParticipantSelectionTable.svelte`, a checkbox table so a manager can add several members in one go. Columns are federation number, full name, then one column per `matchCategories` entry (the match's own selected category order, not the tenant's full category list — mirrors the existing `categoryLabel()`/manual-add-form category columns already in this view). A single text filter matches name or federation number. Members already in the match are always shown regardless of the filter, sorted by name, pinned after the (also name-sorted) not-yet-added rows, tinted (`.assigned-row`, light green `#eaf3e9` — a deliberately different, positive-toned color from the existing red `.unlisted-row`/`.scope-conflict-warning` tint used for warnings elsewhere in this file), and rendered with a checked+disabled checkbox — this view is add-only, unchecking an already-assigned row does not remove it (removal still goes through the existing per-participant remove flow in `MatchParticipantView`). Checking one or more not-yet-added rows and clicking "Apply" (`labels.applyLabel`, already existed) calls `api.addMatchParticipant` once per newly-checked id in sequence, then invokes `onApplied` (wired to `MatchDetailView`'s existing `onChanged`) to refresh — the component does not auto-close the surrounding `showAddForm` card afterward (unlike the old one-shot dropdown), since the whole point of a bulk tool is letting the manager keep browsing/checking without re-opening the form after every add. `MatchDetailView`'s old `sourceMemberId`/`availableMembers`/`submitAddFromList` were removed entirely; the separate "+ Add unlisted participant" manual-entry card (for `allowFreeParticipants` matches) is unchanged and still sits below this table.
- **Copy match (2026-09-13)**: `MatchDetailView`'s "⋯" actions menu gained a "Copy match" item, following the exact same client-side-rebuild pattern `CompetitionDetailView.copyCompetition()` already established (there's no backend copy endpoint for either entity - the frontend reads the full entity and re-issues normal create calls). Unlike the competition copy (which fires immediately), this one needs a yes/no choice first ("with or without participants"), and since this codebase has no custom-modal convention (native `confirm()`/`alert()` only, see the session-expiry-watcher entry above), clicking the menu item just toggles open an inline `.entry-card` (the same expand-in-place pattern `showAddForm` already uses lower on this page) with a checkbox (`copyIncludeParticipants`) and its own Copy/Cancel buttons, rather than acting immediately or opening a real dialog. `copyMatch()` in `MatchDetailView.svelte`: creates the new match via `api.createMatch` with the same `MatchInput` fields (name prefixed via `labels.copyOfMatchName`, `isOpen: false` per the same default new matches already use in `MatchesView`), then replays `devices` (`api.addDevice`, preserving sort order) and `liveScopes` (`api.addLiveScope`, `groupByCategoryIdsJson` parsed back into the array the endpoint expects) onto it unconditionally, then - only when the checkbox was ticked - replays `participants` (`api.addMatchParticipant`, same shape used elsewhere for list-linked/free participants) and re-applies each one's device assignment via a `deviceIdMap` from old device id to newly-created device id. Deliberately not copied: entered scores (a "copy" is for reusing a match's setup/roster next time, not replaying last time's results) and `isOpen` (copies always start closed, same as any new match). `onCopied: (match: Match) => void` is a new required prop threaded through from `App.svelte` exactly like the competition one (`onCopied={(copy) => { loadMatchesList(); openMatch(copy) }}`).
- **Live scope "always show these categories in results" (2026-09-13, backend + frontend)**: a live scope's existing `groupByCategoryIds` checkbox grid (in both `MatchMetadataEditView` and `TemplateEditView`'s live-scopes editor) gained a sibling grid, `displayCategoryIds` (`labels.scopeDisplayCategoriesLabel`), for categories whose value should always render in small print under a participant's name in `line1`/`line2` output - independent of grouping. This is genuinely a backend feature, not achievable frontend-only: `line1`/`line2` in `LiveScoringPage`/`LiveScoringEntry` (`types.ts`) are pre-formatted strings computed server-side by `LiveScoringService.BuildBlocks` in `centaur-scores-api-v2`, shared by both the public narrowcast endpoint and the authenticated match-results-scope preview - the frontend never sees raw participant category values for either. New `LiveScoreScope.displayCategoryIdsJson` column (`AddLiveScopeDisplayCategories` migration) and `CreateScopeRequest.DisplayCategoryIds` field on the API side; `LiveScoringService.CreateEntry` renders the selected categories' values (`" / "`-joined) as the *first* entry in the same `details` list that already carries group-scores/equalizer/personal-best text, so it lands on the same `line2` as the existing tie-breaker info, joined by the same `" | "` separator - e.g. `"Recurve | (19 + 1x10)"`. No `LiveScoringView`/`MatchResultsScopeView` changes were needed on the frontend at all - both already just render `entry.line2` verbatim in `<small>`. `LiveScopeConfig` (the shared match/template local-editing type) and the real `LiveScoreScope` API type both carry the new field; `api.addLiveScope`'s body type, the `addLiveScope()`/`toggleScopeDisplayCategory()` pair in both edit views, `MatchesView`'s template-driven and no-template-fallback scope creation, and `MatchDetailView.copyMatch()`'s scope replay were all updated to thread it through - search for `includePersonalBest` if adding another scope field like this one, since every one of those call sites lists all the scope fields positionally/by name and needs the same update. `templateConfig.ts#parseTemplateConfiguration` normalizes `displayCategoryIds` to `[]` per-scope for templates saved before this field existed (a template's `liveScopes` array isn't otherwise per-field-validated on parse). Verified live end-to-end against the dev tenant: a tied pair of participants showed `"Recurve | (19 + 1x10)"` on both the public `/live-scoring/match/{scope}/{matchId}` JSON and the rendered narrowcast page, and the new checkbox grid round-tripped correctly through `MatchMetadataEditView`'s save (checked → saved → reloaded → still checked, and back).
- **Blocking `alert()` for sticky-actions save/apply result (2026-09-13)**: the `.sticky-actions` save button pattern (see CLAUDE.md conventions) previously gave feedback only via an inline `{#if saveError}`/`{#if saveMessage}` paragraph next to the form, easy to miss since it sits above the sticky bar and doesn't scroll into view on its own. First attempt was a toast (a `writable` store + a fixed-corner `Toast.svelte`), but the user explicitly rejected toast for this: an auto-disappearing, easy-to-miss corner notification is wrong for a "did my save work" confirmation - they want something the user must actively acknowledge before continuing, and rejected navigating back to a parent view instead (ambiguous parent in this system). Landed on a plain blocking `window.alert(message)` call in the `save()`/`apply()` try/catch, since that's already this codebase's established modal-acknowledgment pattern (`SessionExpiryWatcher` already uses `alert()` for its "your session expired" notice, and destructive actions already use `confirm()` - see the session-expiry-watcher entry above; no custom-modal component exists here, deliberately). All six `.sticky-actions` views (`AccountEditView`, `MatchMetadataEditView`, `TemplateEditView`, `TenantEditView`, `TenantSettingsView`, `MatchAddParticipantsView`) call `alert(labels.xSaved)` on success and `alert(labelForError(...))`/`alert(labels.xSaveError)` on failure directly - no shared helper module, matching how `confirm()` is called inline elsewhere. The redundant inline success/error paragraphs tied to that button were removed (an error paragraph tied to a *different* action - e.g. `TenantEditView`'s logo-file validation, `MatchAddParticipantsView`'s manual-add-participant form - was deliberately left as inline `.error` text, since only the sticky save/apply button itself is in scope here). `MatchAddParticipantsView.apply()` previously had no success feedback at all (it silently cleared the selection) - gave it a new `labels.participantsAddedMessage` key. Verified with a one-off Playwright smoke script (no `chromium-cli`/project run-skill in this repo yet) against the dev tenant, using Playwright's `page.on('dialog', ...)` to capture the native alert: logged in, saved Tenant Settings for a real success alert ("Standaard scope opgeslagen."), then intercepted `PUT /api/tenants/current/default-scope` to force a 500 and confirmed the failure alert ("Opslaan van de standaard scope is mislukt.").
- **Add-participants became its own routed page + a global sticky-bottom-actions pattern (2026-09-13)**: `ParticipantSelectionTable` (added earlier the same day as an inline `.entry-card` in `MatchDetailView`) moved to a dedicated `MatchAddParticipantsView`/`match-add-participants` route (`matchAddParticipantsPath(matchId)`, `/matches/{id}/add-participants`) instead of an inline card shown above the (possibly long) existing participant list - there's no UX value in showing both at once, and a real page gets a proper `back-link`/eyebrow/`h1` plus room to show the source list's name (`sourceList.name`, under the heading) per explicit request. `MatchDetailView`'s "+ Add participant" button is now a plain `<a href={matchAddParticipantsPath(...)}>` (`navigateOnClick`, per the navigation convention) instead of toggling local state; it lost `sourceList`/`onChanged` props entirely since nothing in that view read them anymore once the add-flow moved out (svelte-check's `export_let_unused` warning caught both). `ParticipantSelectionTable` itself was demoted from an API-owning widget to a **presentational** component: it no longer takes `api`/`matchId`/`onApplied` or owns `selectedIds`/apply state - just `members`, `categories`, `assignedMemberIds`, `selectedIds`, `onToggle(memberId)`. The owning page (`MatchAddParticipantsView`) holds `selectedIds`/`applying`/`applyError` and the `apply()` loop (`api.addMatchParticipant` per selected id) itself, because the Apply button needed to live in a **page-level sticky action bar** (see below) alongside a Cancel button, not bundled inside the table widget. **New global `.sticky-actions` class** (`app.scss`, right after the `.primary` button rules): `position: sticky; bottom: 0` with a solid `var(--neutral)` background and top border, so a page's bottom action row (Save, or here Cancel+Apply) stays reachable without scrolling past a long form/list - reuse this class instead of a plain trailing button on any settings/configuration page. Applied so far to: the add-participants sticky footer (`Cancel` always, `Apply` only when there's a source list to select from - `selectedIds.size === 0 || applying` disables it), and the single-Save-button settings pages `MatchMetadataEditView`, `TemplateEditView`, `TenantEditView`, `TenantSettingsView`, `AccountEditView` (moving their trailing `<button>{labels.save}</button>` out of the page's `<section class="panel">` into a `.sticky-actions` div below it). **Deliberately left alone** (per explicit scoping decision): pages that mix a top-level settings form with list-management mini-forms rather than being one clean settings-form-with-one-save-button - `ProfileView` (two separate forms/saves), `CategoryDetailView`/`ParticipantListDetailView`/`CompetitionDetailView`/`MatchParticipantView`/`PersonalBestClassifiersView`/`PersonalBestDisciplinesView` (per-row inline add/edit saves, not one page-level save). **Form-linked button gotcha**: where the Save button previously sat *inside* a `<form on:submit|preventDefault={save}>` (`TenantEditView`, `TenantSettingsView`, `AccountEditView`), moving it out into the sticky bar would have broken both the click *and* Enter-to-submit-in-a-text-field behavior - fixed by giving the `<form>` an `id` (e.g. `id="tenant-edit-form"`) and using `<button type="submit" form="tenant-edit-form">` in the sticky bar; the HTML form-owner mechanism treats a `form`-attributed button as part of that form for both explicit clicks and a text input's implicit Enter-submit even though it's not a DOM descendant - confirmed both paths still work via a Playwright check (click outside the form, and `input.press('Enter')`) at a 390×640 viewport where the sticky bar sat within the viewport with no page scroll needed. `MatchMetadataEditView`/`TemplateEditView` never used a `<form>` element in the first place (plain `on:click={save}` button), so no `form=` linking was needed there.
- **Scorecard signing, web-ui phase (2026-09-20)**: see `../documentation/SIGNING-SCORECARDS.md` for the full cross-repo feature; this is Phase 1 (config + status/toggle + CSV language param), Phase 2 (Flutter/mobile-web-scoring signing UI) and Phase 3 (print scorecards) are separate follow-ups not yet implemented. `Match`/`MatchTemplate.signatureMode` (`'none' | 'confirm' | 'signature'`, new `signatureModes` const in `templateConfig.ts`, same pattern as `deviceSelectionModes`) got its own `<select>` in the first `<section class="panel">` of both `MatchMetadataEditView`/`TemplateEditView` (per spec wording), and is threaded through the same create/copy-from-template paths `deviceSelectionMode` already goes through (`MatchesView.submitAdd`, `MatchDetailView.copyMatch`, `TemplatesView.submitAdd` defaults new templates to `'none'`). `MatchParticipant` gained `signed`/`signedAtUtc`/`archerSignatureDataUrl`/`markerSignatureDataUrl` (all rendered by the API alongside the rest of the participant, no new fetch needed). The sign/unsign toggle **first landed as a per-row button in `MatchDetailView`'s participant list, then got moved into the status box on `MatchParticipantView` per explicit user feedback** ("should not be in the list view" — the list view only keeps the read-only `.signed-badge`, which the user did want). `MatchDetailView` never ended up needing a `canManage` prop at all — it was added then removed again once the toggle moved out. The toggle now lives inside `.signature-status-banner` on `MatchParticipantView` (flex row: status text + `.sign-toggle-button`, gated on the existing `canManage` prop that view already had for other things), calling the same `api.signMatchParticipant`/`unsignMatchParticipant` methods. Always goes through `confirm()` first (this codebase's only confirmation pattern), then optimistically patches the local `participant` object and calls `onChanged()` so the parent's match data (and thus the list view's badge) refreshes too — no full page reload. `.sign-toggle-button` is a shared `app.scss` class (not view-local) since it's used inside the shared `.signature-status-banner`, which also gained `display:flex`/`margin-bottom:16px` (was margin-top only) per a second piece of feedback asking for visible space between the status box and the panel below it, on both `MatchParticipantView` and `MatchParticipantScoreView`. The group-by menu gained a `'signed'` option (only shown when `match.signatureMode !== 'none'`) whose ordering is a **hand-built two-bucket array with unsigned always first**, not routed through the existing alphabetical-with-unassigned-last `groupedParticipants` sort logic used for category/device grouping — the spec explicitly wants unsigned surfaced first as the "needs attention" bucket, which inverts that convention. `MatchParticipantView`/`MatchParticipantScoreView` both gained a status banner (new shared `.signature-status-banner`/`.is-signed` classes hoisted into `app.scss` next to `.pb-tile-*`, rather than duplicating the same three-line style block in both components) and a `canEditScores = !participant.signed || canManage` gate on their edit affordances (quick-set button, the scores-panel pencil-edit link, and `ParticipantScorecard`'s `interactive` prop in the dedicated score-entry view) — **neither view gated score editing on `canManage` at all before this change**, this is new protection matching the equivalent new backend guard on `EnterScore`, not an extension of a pre-existing pattern. Signed images (when present) render below the read-only scorecard preview in `MatchParticipantView`'s existing expand-on-demand block. `api.downloadMatchExport` gained a required `language: Language` param (new `?language=` query string on `export.csv`, mirroring the participant-list Excel export) so the new "Signed"/"Ondertekend" CSV column localizes — `MatchDetailView.exportCsv()` now passes its own `language` prop through.
- **Narrowcast results grid rewritten as a JS-computed deterministic layout, not CSS multi-column (2026-09-13)**: `LiveScoringView.svelte`'s `.live-results` area was rebuilt per `../documentation/NARROWCASTING-DEFINITION.md` (see DESIGN.md's UC17 for the summarized spec) to guarantee 48 participants + their category headers always fit without vertical scrolling, regardless of screen resolution. Two non-obvious pitfalls surfaced via a Playwright-driven smoke test against the dev tenant (`claude`/`claude`) and are worth knowing before touching this view again: **(1) native CSS multi-column (`column-count`/`column-fill:auto`/`break-inside:avoid-column`) silently disagrees with row-count arithmetic** - the browser's own column-breaking can strand a header or push a row into a column past what the code renders as scrollable, making it invisible with no error. Fixed by computing column assignment ourselves in JS (`partitionColumns()` walks blocks/entries and assigns each header/entry to an explicit column array, filling one to its row capacity before starting the next) and rendering explicit `.result-column` flex children instead of relying on the browser to paginate - column count and scrollable width are now always in exact agreement with what's actually placed. **(2) Font size cannot be derived from row height alone** - a sparse match (few participants) gets a very tall grid row, and scaling the font to fill that height overruns the column's *fixed width* long before it runs out of vertical room, either overlapping the numeric (position/average/score) grid columns or truncating names/headers mid-word. Fixed by capping each font independently by an assumed character budget (`NAME_CHAR_BUDGET=20`, `HEADER_CHAR_BUDGET=24`) against the column's real width, taking `min(heightDerivedFont, widthDerivedCap)` - any leftover row height becomes vertical padding rather than an oversized, clipped font. The width-cap math depends on two glyph-width constants (`CH_ESTIMATE` for the `ch`-unit/tabular-digit grid tracks, `TEXT_CH_ESTIMATE` for proportional name/header text) that must be calibrated against the app's actual rendered font (DM Sans) via `canvas.measureText` - generic assumptions (~0.5em) undershot the real values (~0.675em/~0.588em) by 15-20%, and since the numeric font size is solved for by *inverting* the `CH_ESTIMATE` ratio against a fixed px budget, underestimating it doesn't just mis-size the numeric columns - it also feeds a too-generous "space remaining" figure into the name-column budget, so both bugs shared one root cause. If the app's font ever changes, re-measure and re-tune `CH_ESTIMATE`/`TEXT_CH_ESTIMATE` (a small `canvas.measureText` script against the live page is the fastest way, as opposed to guessing). `computeRowsPerColumn()` also has a subtlety: the naive `ceil(units / columns)` division assumes perfect bin-packing, but keeping a header attached to its first entry (so a column never ends on a bare header) can strand a spare unit - so it actually runs the real `partitionColumns()` against the first-48-entries window and grows the row count until that window fits the baseline column count, rather than trusting the arithmetic alone. **Capacity is always assumed to be the full 48 participants, never the match's real entry count** - the first cut of this sized rows from `min(actualEntries, 48)`, which seemed more "space-efficient" for small matches but the user rejected it on sight: a 6-participant match got a handful of enormous rows, which looked broken rather than nice. `computeRowsPerColumn()`'s `capacityUnits` is `CAPACITY_ENTRIES + capBlocks.length` (a fixed 48 plus however many category headers are in the first-48 window) unconditionally, so unit height/font size are consistent across matches of any size and vary only with category count, not participant count.
