${CENTAUR_LOG_DIR}/nginx-*.log {
    size ${LOG_MAX_FILE_SIZE}
    maxage ${LOG_RETENTION_DAYS}
    rotate 1000
    missingok
    notifempty
    compress
    delaycompress
    postrotate
        [ -f /run/nginx.pid ] && kill -USR1 "$(cat /run/nginx.pid)" 2>/dev/null || true
    endscript
}
